# -Simple-Sine-Wave-Plot
This script plots a sine wave using Python’s NumPy and Matplotlib libraries. It generates 100 points from 0 to 2π, computes the sine values, and visualizes the curve with labeled axes, a title, and grid lines. Ideal for learning basic plotting in Python.
